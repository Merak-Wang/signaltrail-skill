# Technical-Debt Tracker

**Purpose:** Keep known implementation and audit gaps visible with evidence and concrete next actions.
**Status:** Verified
**Owner:** Repository maintainers
**Last verified:** 2026-08-25

| ID | Priority | Gap | Evidence | Next action |
| --- | --- | --- | --- | --- |
| TD-001 | High | The tracked <code>skills/signaltrail/</code> release snapshot has drifted from canonical root sources. | File hashes differ across implementation, references, configs, and templates. The active Hermes installation was separately migrated and now matches all 31 canonical Python modules. | Rebuild the tracked snapshot only during an explicit release; do not hand-edit it or mix release regeneration into ordinary implementation work. |
| TD-002 | Medium | Validation and rendering are concentrated in very long functions. | `validate_report_data`, `compile_report_data`, `render_report_markdown`, and `report_to_blocks` are the largest functions. | Add characterization tests, then extract rule/render families without changing schemas or output. |
| TD-003 | Medium | Detailed `references/` are primarily Chinese while runtime entry documents are English. | The records are useful but language authority is implicit. | Translate only when touched; keep English engineering summaries and do not duplicate runtime policy. |
| TD-004 | Low | CLI command dispatch remains large despite shared JSON output/read helpers. | `main()` still owns many independent commands. | Move cohesive command families to typed handlers after command-level characterization tests exist. |
| TD-005 | Medium | The zero-model monitor labels core sources with specialized adapters such as Weibo as `unsupported`, even though the formal edition collector can fetch them. | The 2026-08-05 monitor showed `weibo_hot` as unsupported while the formal index recorded `success` with 50 items; `pboc_monetary_reports` and `bytedance_seed_papers` share the boundary. | Add a neutral `collector_only` monitor capability/status (or an equivalent explicit capability field), keep it out of failure-rate counters, and label it as “edition collector only” without invoking browser/specialized collection from the monitor. |
| TD-006 | High | A process-wide enrichment failure can strand a run in <code>extracting_content</code>, which no normal entry point resumes. | <code>enrich_edition</code> persists the state before an unguarded <code>extract_content</code> call, while ordinary preparation returns an existing non-terminal run unchanged. | Make enrichment checkpointed and re-entrant, preserve index lineage, and add failure-injection tests after the state transition and during immutable index creation. |
| TD-007 | High | Authoring packets are mutable and are not cryptographically bound to the immutable context or authoring session. | Packets use overwrite-capable writes; the session hashes only the main context, and submission validates against the packet currently on disk. | Create packets immutably, persist each packet hash and authorized item IDs in the context/session, and verify them during begin, submit, and recovery. |
| TD-008 | High | Source-cache health and authoring-session lineage can be overstated after state changes. | Cached items can produce formal <code>success</code> while monitor acquisition carried a partial/error state, and enrichment remains allowed after an authoring session has been dispatched. | Preserve acquisition health separately from item availability; reject or explicitly invalidate authoring sessions when enrichment rebuilds their bound context. |
| TD-009 | Medium | Context compaction can omit an explicitly enriched item below the per-source context cap. | <code>_compact_candidates</code> expands the prefix by the count of enriched items rather than the greatest enriched position, so one enriched item below rank 25 can still be excluded. | Union explicitly enriched evidence into the bounded context while preserving immutable Top selection order, and add a rank-26-or-lower regression test. |
| TD-010 | Medium | Report persistence still lacks a revision transaction, and divergent evaluator attempts share one mutable draft path. | JSON can persist without its Markdown peer. Evaluation revision allocation, recovery, current-report gating, and projection now share the edition lock, but two Agent attempts may still overwrite the same pre-save draft before either enters that lock. | Add report-revision transaction records and immutable per-attempt evaluation drafts; retain the current numeric revision, stale-report, and crash-recovery invariants. |
| TD-011 | Low | Acquisition-path telemetry remains coarser than other authoring evidence. | Immutable bounded rejection receipts now retain brief/analysis attempt Hashes, field/rule errors, usage tasks, repair budget, and authorization; analysis shape/evidence is checked before assembly. Live acquisition is still combined as <code>browser_or_http</code>, and legacy batch duration may start at the session timestamp when host dispatch telemetry is absent. | Separate acquisition-path metrics and prefer host dispatch timestamps when available without inventing missing latency. |
| TD-012 | Medium | Monitor snapshot eligibility and projection-ready milestones are not enforced by one shared truth check. | Preflight checks future time, token use, and structure more strictly than direct monitor loading; HTML failure can still be followed by an unconditional ready milestone. | Share one monitor-snapshot validator and derive readiness milestones only from confirmed artifact existence. |
| TD-013 | Medium | <code>published_at</code> orders only the bounded rows returned by an adapter. | An adapter can truncate its page/feed before the shared sorter sees older/newer rows, so the result is “newest within the fetched subset,” not necessarily newest across the source. | Define adapter acquisition depth for time ordering, expose truncation telemetry, and characterize pagination/window behavior. |
| TD-014 | High | Standalone <code>save-report</code> can compile without the run-owned brief-plan boundary. | The command accepts an index and draft but does not require the context plan; callers outside <code>finalize_edition</code> can therefore bypass exact <code>default_item_ids</code> enforcement. | Require a context/plan artifact or make the standalone command explicitly diagnostic-only; add an out-of-plan rejection test at the CLI boundary. |
| TD-015 | Medium | Verified multi-page source merging lacks sufficient behavior characterization. | Ordering, duplicate replacement, and provenance rules are implemented across capture and merge paths but are not tested for enough multi-page/retry combinations. | Add page-order, duplicate, partial-page, and retry fixtures before refactoring the merge path. |
| TD-016 | Medium | The nominal 45-item authoring batch size is a soft balancing target. | Whole-source grouping can create a packet above 45 items, so downstream output/token limits are not a strict invariant. | Persist an explicit hard packet cap or document/validate the maximum overshoot allowed by whole-source grouping. |
| TD-017 | Low | PDF image resampling now has budgets and visual regression, but production-scale improvement is not yet measured. | Both Edge and ReportLab use the 1600×1000/quality-82 print projection, outputs record render seconds/bytes against a 50 MiB soft budget, PDF is deferred, evaluation refresh reuses it, and Poppler raster smoke coverage passes. No comparable production report has yet measured the new first-render size and latency. | Measure the next image-heavy report, retain page inspection evidence, and revise the print envelope only if readability or the size budget fails. |
| TD-019 | Medium | Narrative continuity and inline evidence references are not fully machine-bounded. | Structured evidence IDs validate, but prose can still mention a brief outside featured-event evidence; <code>change_from_prior</code> can anchor to a non-adjacent report. | Extract/validate inline item IDs and bind continuity authoring to the immediately preceding eligible report ID and claims. |
| TD-020 | High | Hermes Cron cannot yet route evaluator per-request hooks into the task-specific child ledger. | Direct isolated v2 evaluators retained pre/post/error hooks and evaluation-attempt lineage through successful, failed, repair, and recovery calls. The scheduled 2026-08-25 evaluator again emitted no task-routed leaves; its exact 27-call / 1,591,283-token session aggregate had to be imported into the hash-bound child task. The installed Cron CLI still exposes no job-specific environment/tool schema before the first provider call. | Add an audited Hermes plugin/job environment mapping, then verify leaf coverage in a scheduled evaluator without rewriting the direct or Cron ledger. |
| TD-021 | Medium | Codex and OpenClaw usage adapters lack sanitized fixtures captured from known real host versions. | Codex cumulative-counter and OpenClaw current SQLite schema-v17 parsers have synthetic regression fixtures and fail visibly outside audited layouts, but host formats can change. | Capture minimal secret-free real fixtures from explicitly identified versions, update the audited boundary deliberately, and retain fail-closed parsing. |
| TD-023 | Medium | Provider cost and tool-call token splits remain unavailable on hosts that do not expose them. | The ledger correctly records these fields as <code>unobservable</code>; a Hermes hook may expose tool-call count but not tool-call output tokens, and provider receipts often omit billed cost. | Preserve exact host fields when present; otherwise add only versioned, auditable estimates with price-table provenance and never relabel an estimate as provider-reported usage. |
| TD-025 | High | Several workflow mutators read a run before acquiring the edition lock and can later commit stale attempt/artifact state. | Begin/prepare-analysis/assemble/enrich/finalize paths can wait on the lock while retaining a pre-restart run object; <code>adopt_index_for_run</code> lacks the same lock boundary. | Re-read and validate run attempt plus artifact lineage inside the lock, use attempt/context-hash compare-and-swap around long work, and add deterministic restart-race tests. |
| TD-026 | Medium | Durable-host import scope and file-size boundaries are not yet task-selective. | OpenClaw SQLite import accepts only audited schema v17 but currently reads every usage-bearing transcript row in the supplied per-agent database; Codex JSONL import has a fixed 64 MiB safety cap. | Add explicit agent/session/time filters and streaming Codex parsing with bounded records; keep call count unknown when the host does not expose provider-attempt count. |
| TD-029 | High | Secure compiler-owned event IDs lack a verified cross-item continuation mechanism. | Analysis drafts can no longer forge <code>event_id</code> or <code>source_refs</code>, and Python derives identity from authorized current item IDs; a genuinely new article that updates an older event cannot yet declare that lineage safely. | Define a bounded prior-event candidate set and validated update/supersession field, verify source lineage and status transition in Python, and test both forged-history rejection and legitimate cross-day updates. |
| TD-030 | High | Hermes delegated workers inherit the parent's full toolsets. | The installed `delegate_task` implementation explicitly rejects model-selected/narrowed toolsets and passes `toolsets=None`. The v2 acceptance did verify direct one-shot brief workers with only the `file` toolset and fixed packet/output paths, but that does not remove browser/search/delegation schemas from delegated child requests. | Add host-supported per-child toolset selection, then repeat the narrow brief-worker A/B through actual delegation and verify request schemas, acceptance, tokens, calls, and wall time. |
| TD-031 | High | Controlled optimization evidence fails the stable quality/lifecycle gate beyond one analysis-phase A/B. | The frozen v2 A/B produced two zero-error/zero-warning reports and an exact analysis reduction from 584,418 to 174,588 tokens (-70.1%), but its recovered matrix scored 31/33/33/31 below the recorded floors. The 2026-08-25 production report repaired real-report quality to 37/45 with `accept` continuity, yet it changed model/snapshot, missed the deadline, and retained three open calls; it is quality evidence, not a controlled batch/model or stable-lifecycle result. | Run independent single-variable batch-size and phase-model trials plus at least one comparable clean lifecycle. Preserve the rejected v2 matrix and do not relabel the production pass as its acceptance. |
| TD-032 | Medium | Root-like provider turns remain a host/orchestration concentration. | The baseline root-like session used 64 calls and 3,865,416 tokens. The initial 2026-08-25 coordinator used 33 calls and 1,785,325 tokens, below the provisional call target, while the complete recovered foreground attempted 108 calls and retained three open lifecycles. The changed model, live snapshot, and recovery prevent a comparable root-only claim. | Measure repeated context/tool-schema/polling categories in comparable root-session leaves and accept changes only if calls fall to at most 51 without quality regression or open lifecycles. |
| TD-033 | High | A successful Hermes one-shot can leave pre-hook calls without a post/error terminal event. | The v2 control brief produced 24 attempted calls but only 22 token-accounted terminal observations; both missing terminals remained absent after the process exited and made the task total an explicit lower bound. Finalization correctly stayed partial, but the host supplied no safe terminal reason or usage. | Add host-side cancellation/transport terminal hooks or a durable request reconciliation source; keep such tasks partial and never impute missing output, cache, total, tool calls, or latency. |

Resolved in the 2026-08-02 audit:

- Replaced the wall-clock-dependent monitor fixture with an injected clock.
- Unified JSON, text, and byte atomic writers with collision-safe temporary names.
- Made immutable JSON creation no-overwrite even under concurrent writers.
- Centralized typed JSON-object reads and repeated CLI JSON output.
- Added semantic Chinese logic/input/output contracts to every maintained Python definition.
  Inputs name provenance and consumed fields; outputs explain downstream meaning. Critical
  boundaries carry inline rationale, and an AST gate rejects empty template wording.

Resolved during the 2026-08-05 regeneration:

- Excluded retained monitor-history rows from formal report eligibility.
- Made successful live acquisition authoritative before monitor tail fallback.
- Preserved original source-group positions when merging retried sources.
- Added validation-only report identity without mutating authoring drafts.
- Bound evaluation finalization to canonical source with a shell-neutral Python launcher and
  the absolute canonical report contract.
- Installed the canonical <code>signaltrail</code> runtime, verified all 31 Python modules, and
  removed three legacy <code>daily-intelligence</code> skill copies to the Windows Recycle Bin.

Resolved during the 2026-08-23 LLM-usage audit:

- Added the local immutable usage-event layer, host-specific allowlist adapters, unknown-preserving
  summaries, and verified <code>run.llm_usage</code> task bindings without storing raw model content.
- Changed independent evaluator scheduling from three repeated executions to one idempotent
  execution and selected the canonical <code>signaltrail</code> skill instead of the legacy
  <code>daily-intelligence</code> name.
- Made evaluation saves return the current completed revision for the same report/hash and
  semantic draft, recover an interrupted latest revision, and allocate a new numeric revision for
  changed or non-current history. Shared edition locking and current-report gating prevent stale
  evaluators from advancing current projections; today's r1/r2 records remain preserved.
- Added explicit phase/batch/Agent/run/repair/evaluation/parent-session correlation, lineage
  coverage summaries, packet correlation hints, and CLI import flags; raw parent IDs remain hashed.
- Enforced observed-plus-reserved LLM budget checks before brief, analysis, repair, and evaluation
  work while keeping unmetered usage null instead of zero.
- Split cached content semantics from run-relative status/importance, added per-reason cache
  metrics and versioned targeted invalidation, and preserved current-plan ordering.
- Migrated fixed analysis domains to stable IDs, preserved legacy rows as superseded history, and
  closed orphaned watchers without truncating the authoritative state.
- Added immutable bounded brief/analysis rejection receipts, current-report evaluation preflight,
  scheduler reconciliation, at-most-two evaluator attempts, and a minimal immutable hash-bound
  evaluator dossier.
- Made brief and analysis packets self-contained with receiver-enforced output schemas, nested
  object/type/enum rules, additional-property denial, and explicit Python-owned field boundaries.
