# 报告 A 第二部分：代码审阅处理记录

**目的：** 记录已核实缺陷、修复和研究建议的能力边界。
**状态：** 历史记录
**负责人：** 仓库维护者
**最后验证：** 2026-09-11

[English](../../exec-plans/completed-2026-09-11-report-a-code-review.md)

用户提供的报告 A 审阅了提交 `c324c5e8529ae22268b2bd53a7b8394f3f8af26f`。
本次针对第二部分核对当前工作区，保留其中已有的实验讲解功能改动。
研究报告原文件和生产数据不复制到仓库。

| 审阅项 | 处理及依据 |
| --- | --- |
| A01 | 修复正文压平。`content_extraction` 在 JSON 中保留有序标题、段落、列表和表格单元格及跨行跨列声明；Markdown 从同一批块派生。每次抽取使用唯一产物路径。 |
| A02 | 修复短公告丢失及仅凭长度标记 `full_text`。HTTP 与浏览器共用区域选择、正文块及链接密度检查、不完整提示和 HTTP 截断标记。短而具体的正文可完整保存；宽泛或截断内容最多为部分正文。分类仍为结构启发式（TD-040），不代表事实核验。非 HTML HTTP 错误同样保留访问失败。 |
| A03 | 修复忽略描述符而倒序，以及 Feed、静态索引、浏览器索引中 `src` 抢先的问题。有效 `w/x` 在同单位内降序，同值稳定；混合单位保持组间顺序。浏览器索引保留当前图片源与自然尺寸，下载解码仍提供真实宽高。裁切及目标适配属于 TD-040。 |
| A04 | 为选中正文的配图保留图注、替代文本、邻接段落、用途和词法关联依据。有上下文图片优先于通用元数据；无标签图片不替换发布者元数据配图。宽泛页面、推荐区和明确装饰图不作为正文候选。不声称已完成模型视觉核验（TD-040）。 |
| A05 | 报告已明确这是产品约束。普通日报继续遵守 Top 顺序和最多 12 篇正文补全契约；研究通道属于单独的后续能力。 |
| A06 | 能力边界。监控聚类继续用于词法线索发现；`source_count` 是来源 ID 数，不是独立核验次数。正式报告事件仍绑定文章。跨语同事件验证需要单独语料和设计。 |
| A07 | 修复持久状态中的领域/论点碰撞。报告 `analysis_id` 继续代表栏目；状态 schema 1.2 增加判断与证据绑定的 `thesis_id`。不同判断/证据分别保留历史，`analysis-domains.json` 提供最新栏目投影；旧歧义记录保留并标记。 |
| A08 | 修复因未提及而关闭观察项，以及空白排版导致的重复身份。观察 ID 绑定具体论点和规范化信号文字；只有同一论点明确关闭时才关闭其观察项。跨措辞的结构化触发条件和受验证延续仍属于 TD-039。 |
| A09 | 最近五份报告是有界连续性输入。任意日期/实体的长期检索是独立能力；本次没有用扩大提示窗口冒充实现。 |
| A10 | 报告提出的是信息架构建议，没有提供可测 UI 缺陷；本次未重写通用日报界面。 |
| A11 | 所引用的 TD-002/007/010 仍未解决。拆出抽取函数和正文路径防碰撞，不代表已解决写作包绑定或成对报告事务。 |
| A12 | 所引用路线图早于当前实验讲解改动。保留已有快照预览工作；实时新闻准入和正式媒体验收仍受 TD-038 限制。不声称已经交付视频能力。 |

行为回归覆盖描述符排序、三条采集入口、短公告、链接农场、隐藏/推荐内容、行内词汇、
表格跨列、截断、图文上下文、防碰撞正文路径、旧状态、多条同领域论点、观察项遗漏、
明确关闭、迟到报告、评估排除、数据包身份以及共享状态锁。

完整仓库门禁使用已有 `.venv` Python 环境。另以离线 HTML Fixture 运行无界面的 Edge
浏览器检查，覆盖浏览器正文读取、CSS 隐藏文字和结构化持久化。这些检查没有测量真实
网站抽取准确率，也没有运行带凭据的生产日报。

最终验证：**453 项测试全部通过，用时 49.00 秒**。Ruff、compileall、中文代码注释检查、
文档检查和 `git diff --check` 均通过。最终离线 Edge Fixture 也通过了样式表位于 body
内部时的隐藏内容检查。

前两轮全量门禁还发现已有讲解目录提交的间歇性 Windows `WinError 5`。现仅对 Windows
访问/共享锁错误执行有限重试；持续错误仍失败，已存在目标绝不替换。故障注入测试覆盖
短暂占用恢复、重试耗尽以及重试间出现目标目录。此修复保留已有讲解行为，不代表解决了
TD-010 的报告事务缺口。

运行策略见[系统契约](../../../references/system-design.md)、
[编辑策略](../../../references/editorial-policy.md)与
[报告契约](../../../templates/report-contract.md)。待验收能力记入
[技术债追踪器](tech-debt-tracker.md) TD-039 和 TD-040。

响应式图片解析依据 [HTML 标准](https://html.spec.whatwg.org/multipage/images.html#parse-a-srcset-attribute)
区分描述符并读取 URL；它是确定性的采集优先级，不是浏览器版式或图片裁切选择引擎。
